TELEGRAM_BOT_TOKEN=
# Create the token with @BotFather in Telegram.
# Optional fallback. If empty, the bot will auto-bind to the first channel
# where it receives membership or channel post updates.
DEFAULT_CHANNEL_ID=
# Optional. If empty, the first private /start will lock the bot to that user.
OWNER_USER_ID=
TIMEZONE=Europe/Kyiv
PUBLISH_TIME=20:30
POST_PROMPT_PATH=content/prompts/post_prompt.txt
AI_PROVIDERS_PATH=config/providers.json
